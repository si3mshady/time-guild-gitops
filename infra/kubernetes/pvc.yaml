apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: timeguild-db-pvc
  namespace: timeguild-prod
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
